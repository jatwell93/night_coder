services:
  wiremock-pilot:
    # Fully qualified for Podman (no unqualified-search registries in default registries.conf).
    image: docker.io/wiremock/wiremock:latest
    ports:
      - "3000:8080"
    volumes:
      - ./mappings:/home/wiremock/mappings:ro
