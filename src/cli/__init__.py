"""Command-line entrypoints for operator-facing workflows.

CLI modules wire services together and handle stdout/stderr. Business logic
belongs in ``src/services``; CLI modules are thin adapters.
"""
