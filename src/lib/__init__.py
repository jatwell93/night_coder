"""Shared utilities and validators.

Modules under ``src/lib`` are pure utility code with no side effects at import
time. Services (``src/services``) and CLI entrypoints (``src/cli``) consume
these utilities.
"""
